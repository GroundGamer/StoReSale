/*
import { getTextComment } from './getTextComment'


import type { StateSchema } from 'app/providers'


import { commentsData, commentsEntities } from '../../../__mocks__/commentsData'



describe('getTextComment', () => {
    test('check and test article comments data', () => {
        const state: DeepPartial<StateSchema> = {
            articleComments: commentsData
        }

        expect(getTextComment.selectAll(state as StateSchema)).toEqual(commentsEntities)
    })

    test('should work with empty state', () => {
        const state: DeepPartial<StateSchema> = {}

        expect(getTextComment.selectAll(state as StateSchema)).toEqual([])
    })
})*/
